@page
@{
	Layout = "Shared/_GameLayout";
}

@model CityVilleDotnet.Api.Pages.GameModel

@section Header {
	<script type="text/javascript" src="jquery.min.js"></script>
	<script type="text/javascript" src="flash_integration.min.js"></script>
	<script type="text/javascript" src="swfobject.min.js"></script>
	<script type="text/javascript" src="ruffle/ruffle.js"></script>
}

<div style="width:760px;height:594px;">
	<div id="flashOuterContainer">
		<div id="flashContent">
			<object id="flashapp" name="flashapp" width="754" height="594" type="application/x-shockwave-flash"
			        data="Preloader.swf">
				<param name="allowFullScreen" value="true"/>
				<param name="flashvars" value="@Model.BuildFlashVars()"/>
			</object>
		</div>
	</div>
</div>

@if (Model.EnableCheat)
{
	await Html.RenderPartialAsync("_CheatMenu");
}

<script>
	function getUserInfo() {
		console.log("getUserInfo called");

		return {
			"zid": @(Model.Uid),
			"uid": "@(Model.Uid)",
			"first_name": "@Model.UserName",
			"name": "@Model.UserName",
			"sex": "M",
			"pic_square": "blank.png",
			"level": @(Model.Level)
		};
	}

	function getFriendData() {
		console.log("getFriendData called");

		return @Html.Raw(Model.FriendList);
	}

	function getAppFriendIds() {
		console.log("getAppFriendIds called");
		return [];
	}

	function onGameLoaded(seen, popp, canvas) {
		console.log("Game is loaded " + seen + ", " + popp + ", " + canvas);
	}

	function getAssetOverrides() {
		console.log("getAssetOverrides called");
		return {};
	}

	function getRuntimeVars() {
		console.log("getRuntimeVars called");

		const weatherSettings = {
			"forecast": [1, 0, 0, 0, 0, 0, 0]
		};

		return {
			"WEATHER": JSON.stringify(weatherSettings),
			"CITY_AT_NIGHT_GAME_MODE": 1,

			"MAX_VISIBLE_NPCS": 150,
			"MIN_VISIBLE_NPCS": 10,
			"MAXIMUM_ACTIVE_TAXIS": 5,
			"MAXIMUM_ACTIVE_CARS": 10,

			"SHRINK_RAY_CAP": 100,
			"SUBSCRIPTION_ENABLE_ZDC": 1
		};
	}

	function getExperiments() {
		console.log("getExperiments called");

		return [
			{"name": "cv_global_leaderboard", "variant": 0},
			{"name": "cv_leader_master", "variant": 0},
			{"name": "cv_multipop_migrate", "variant": 1},
			{"name": "cv_defcon_npc_culling", "variant": 3},
			{"name": "cv_CityatNights_Teaser", "variant": 2},
			{"name": "cv_CityatNights_Main", "variant": 0}, // disable it to remove incentivized expansion_cityatnight that is not supported
			{"name": "cv_CityatNights_Priceunlock", "variant": 6},
			{"name": "cv_quest_frame", "variant": 3},
			{"name": "cv_featured_quest_sort", "variant": 3},
			{"name": "cv_hide_even_more_quests", "variant": 3},
			{"name": "cv_questhiding", "variant": 1},
			{"name": "cv_quest_prorated_pricing", "variant": 0},
			{"name": "cv_quest_sale", "variant": 0},
		];
	}

	function getLocaleOverrides() {
		return null;
	}

	function getFlashApp() {
		return document.getElementById('flashapp');
	}

	function sendRequest(a, b, c, d) {
		console.log("sendRequest called: ", a, b, c, d);
	}

	function completeSendRequest(b) {
		console.log("completeSendRequest called:", b)
	}

	ZYFrameManager = {
		"reloadApp": function () {
			console.log("Reload App");
			window.location.reload();
		},
		"navigateTo": function (a, b, c) {
			console.log("Navigate To" + a + " - " + b + " - " + c);
		},
		"openTab": function (a, b, c) {
			console.log("Open Tab" + a + " - " + b + " - " + c);
		},
		"switchToTab": function (a) {
			console.log("Switch To Tab" + a);
		}
	}

	window.RufflePlayer = window.RufflePlayer || {};
	window.RufflePlayer.config = {
		allowScriptAccess: true,
		autoplay: "on",
		unmuteOverlay: "hidden",
		logLevel: "warn",
		polyfills: true,
		allowFullscreen: true,
		wmode: "window",
		deviceFontRenderer: "canvas",
		maxExecutionDuration: 360
	};
</script>
